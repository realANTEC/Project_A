# E2E runs the app in local/mock mode (no Supabase) for deterministic, network-free
# tests against the curated seed feed. This mode file overrides .env.local because
# Vite loads `.env.[mode]` at a higher priority than `.env.local`.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
